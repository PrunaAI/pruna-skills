# Examples

Starter prompts and repo-only demo scripts. Mirrors `guides/workflows/` layout.

```text
examples/workflows/
  core/
    visual-transition-reel/
  verticals/
    interactive-explainer/
    music-video/
  launches/
    p-image-upscale-comparison/
    p-video-animate-comparison/
    p-video-replace-comparison/
```

`install_skill.sh` copies from `core/`, `verticals/`, or `launches/`.
